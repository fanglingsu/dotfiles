#!/usr/bin/env bash
# Read javascript from stdin and print it minified to stdout.

cat $1 | \
    tr '\n\r\t' ' ' | \
    sed -e 's:/\*[^*]*\*/::g' \
    -e 's|[ ]\{2,\}| |g' \
    -e "s|[ ]\{0,\}\([-!?<>:=(){};+\&\"',\|]\)[ ]\{0,\}|\1|g" \
    -e 's|"+"||g'
